package racingcar.model;

public class TryCount {
    private Long tryCount;

    public TryCount(Long tryCount) {
        this.tryCount = tryCount;
    }

    public Long getTryCount() {
        return tryCount;
    }

    public void setTryCount(Long tryCount) {
        this.tryCount = tryCount;
    }
}
